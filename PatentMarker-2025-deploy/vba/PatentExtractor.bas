Attribute VB_Name = "PatentExtractor"
Option Explicit

' 自 v4.9 起，字典导出统一由 PatentDictPanel 工具面板提供：
'   - 手动导出：面板"手动导出字典"按钮 → AutoExport.ExportDict
'   - 自动导出：面板"保存时自动导出"开关 → AutoExport.IsAutoExportEnabled
' 本模块仅作占位保留（部署包与 Word 端依赖固定文件名，不可删除/重命名）。
